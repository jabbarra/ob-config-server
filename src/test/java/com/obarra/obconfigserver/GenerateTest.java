package com.obarra.obconfigserver;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class GenerateTest {

    @Test
    void name() {
        Generate generate = new Generate();
        //assertEquals("name",generate.name());
    }
}